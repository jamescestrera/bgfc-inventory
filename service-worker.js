const CACHE = 'bgfc-ims-v17';
const ASSETS = ['./', 'index.php', 'dashboard.php', 'categories.php', 'items.php', 'stock-in.php', 'stock-out.php', 'inventory.php', 'suppliers.php', 'low-stock.php', 'adjustment.php', 'transactions.php', 'reports.php', 'users.php', 'audit-logs.php', 'assets/css/app.css', 'assets/css/assistant.css', 'assets/css/transitions.css', 'assets/css/reports.css', 'assets/css/notifications.css', 'assets/css/footer.css', 'assets/js/app.js', 'assets/images/bgfc-logo.png', 'assets/images/bgfc.png', 'manifest.webmanifest'];
self.addEventListener('install', e => e.waitUntil(caches.open(CACHE).then(c => c.addAll(ASSETS)).then(() => self.skipWaiting())));
self.addEventListener('activate', e => e.waitUntil(caches.keys().then(keys => Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k)))).then(() => self.clients.claim())));
self.addEventListener('fetch', e => { if (e.request.method !== 'GET' || new URL(e.request.url).pathname.endsWith('/api.php')) return; e.respondWith(caches.match(e.request).then(cached => cached || fetch(e.request).then(response => { const copy = response.clone(); caches.open(CACHE).then(c => c.put(e.request, copy)); return response }).catch(() => caches.match('./')))) });

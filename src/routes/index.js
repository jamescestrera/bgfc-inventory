const express=require('express');const {body}=require('express-validator');const auth=require('../controllers/authController');const dashboard=require('../controllers/dashboardController');const items=require('../controllers/itemController');const generic=require('../controllers/genericController');const tx=require('../controllers/transactionController');const users=require('../controllers/userController');const reports=require('../controllers/reportController');const {requireAuth,guestOnly}=require('../middleware/auth');const {allow}=require('../middleware/role');const router=express.Router();
router.get('/',(req,res)=>res.redirect(req.session.user?'/dashboard':'/login'));router.get('/index.php',(req,res)=>res.redirect('/login'));router.get('/login',guestOnly,auth.loginPage);router.post('/login',guestOnly,auth.login);router.post('/logout',requireAuth,auth.logout);router.get('/dashboard',requireAuth,allow('dashboard'),dashboard.index);
router.get('/items',requireAuth,allow('items'),items.index);router.get('/items/create',requireAuth,allow('items'),items.form);router.post('/items',requireAuth,allow('items'),items.create);router.get('/items/:id',requireAuth,allow('items'),items.view);router.get('/items/:id/edit',requireAuth,allow('items'),items.form);router.post('/items/:id',requireAuth,allow('items'),items.update);router.post('/items/:id/delete',requireAuth,allow('items'),items.delete);
for(const type of ['categories','suppliers','departments']){router.get(`/${type}`,requireAuth,allow(type==='departments'?'*':type),generic.index(type));router.get(`/${type}/create`,requireAuth,allow(type==='departments'?'*':type),generic.form(type));router.post(`/${type}`,requireAuth,allow(type==='departments'?'*':type),generic.save(type));router.get(`/${type}/:id/edit`,requireAuth,allow(type==='departments'?'*':type),generic.form(type));router.post(`/${type}/:id`,requireAuth,allow(type==='departments'?'*':type),generic.update(type));router.post(`/${type}/:id/archive`,requireAuth,allow(type==='departments'?'*':type),generic.archive(type))}
router.get('/stock-in',requireAuth,allow('stock'),tx.form('STOCK_IN'));router.post('/stock-in',requireAuth,allow('stock'),tx.process('STOCK_IN'));router.get('/stock-out',requireAuth,allow('stock','stock-out'),tx.form('STOCK_OUT'));router.post('/stock-out',requireAuth,allow('stock','stock-out'),tx.process('STOCK_OUT'));router.get('/stock-adjustment',requireAuth,allow('stock'),tx.form('ADJUSTMENT'));router.post('/stock-adjustment',requireAuth,allow('stock'),tx.process('ADJUSTMENT'));router.get('/inventory',requireAuth,allow('inventory'),tx.inventory);router.get('/transactions',requireAuth,allow('transactions','own-transactions'),tx.history);
router.get('/reports',requireAuth,allow('reports'),reports.index);router.get('/reports/:type',requireAuth,allow('reports'),reports.view);router.get('/reports/:type/csv',requireAuth,allow('reports'),reports.csv);router.get('/users',requireAuth,allow('*'),users.index);router.get('/users/create',requireAuth,allow('*'),users.form);router.post('/users',requireAuth,allow('*'),users.create);router.get('/users/:id/edit',requireAuth,allow('*'),users.form);router.post('/users/:id',requireAuth,allow('*'),users.update);router.get('/audit-logs',requireAuth,allow('*'),users.auditLogs);
module.exports=router;

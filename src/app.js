require('dotenv').config();const express=require('express');const session=require('express-session');const flash=require('connect-flash');const path=require('path');
for(const key of ['PORT','DB_HOST','DB_PORT','DB_NAME','DB_USER','DB_PASSWORD','SESSION_SECRET'])if(!process.env[key]){console.error(`Missing required environment variable: ${key}`);process.exit(1)}
const app=express();app.set('view engine','ejs');app.set('views',path.join(__dirname,'views'));app.set('trust proxy',1);app.use(express.urlencoded({extended:false}));app.use(express.json());app.use(express.static(path.join(__dirname,'..','public')));app.use('/assets',express.static(path.join(__dirname,'..','assets')));app.use(session({secret:process.env.SESSION_SECRET,resave:false,saveUninitialized:false,cookie:{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:8*60*60*1000}}));app.use(flash());app.use((req,res,next)=>{res.locals.user=req.session.user;res.locals.success=req.flash('success');res.locals.error=req.flash('error');res.locals.path=req.path;next()});app.use(require('./routes'));app.use((req,res)=>res.status(404).render('errors/error',{title:'Page not found',status:404,message:'The requested page does not exist.'}));app.use((err,req,res,next)=>{console.error(err);if(res.headersSent)return next(err);res.status(500).render('errors/error',{title:'Something went wrong',status:500,message:'The system could not complete your request. Please try again.'})});app.listen(Number(process.env.PORT),()=>console.log(`BGFC Inventory running on port ${process.env.PORT}`));

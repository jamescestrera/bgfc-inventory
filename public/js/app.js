document.getElementById('menu')?.addEventListener('click',()=>document.querySelector('.sidebar')?.classList.toggle('open'));document.querySelectorAll('.confirm-delete').forEach(f=>f.addEventListener('submit',e=>{if(!confirm('Are you sure you want to permanently delete this item?'))e.preventDefault()}));document.querySelectorAll('.confirm-archive').forEach(f=>f.addEventListener('submit',e=>{if(!confirm('Are you sure you want to archive this record?'))e.preventDefault()}));const item=document.getElementById('item');item?.addEventListener('change',()=>{const q=item.selectedOptions[0]?.dataset.qty;const box=document.getElementById('systemQty');if(box)box.value=q??'Select an item'});
